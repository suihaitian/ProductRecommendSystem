spring:
  datasource:
    druid:
      username: root
      password: 527527
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/productsys?serverTimezone=UTC&useUnicode=true&characterEncoding=UTF8&useSSL=false&allowPublicKeyRetrieval=true

  redis:
    database: 0
    host: localhost
    port: 6379
    password:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
    timeout: 500

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cn.demo.redis01.entity
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8083
